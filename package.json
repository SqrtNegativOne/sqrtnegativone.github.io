{
  "name": "site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run media:build && bun run blog:build && vite dev",
    "build": "bun run media:build && bun run blog:build && vite build && node -e \"require('fs').writeFileSync('docs/CNAME', 'sqrt.fyi')\"",
    "blog:build": "bunx @11ty/eleventy",
    "blog:watch": "bunx @11ty/eleventy --watch",
    "media:build": "node scripts/build-media.mjs",
    "lint": "oxlint -c .oxlintrc.json",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "markdown-it": "^14.1.1",
    "markdown-it-footnote": "^4.0.0",
    "motion": "^12.42.2",
    "ogl": "^1.0.11",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.70.2",
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "oxlint": "^1.76.0",
    "svelte": "^5.56.8",
    "vite": "^8.0.1"
  }
}
